pipeline {
    agent any

    stages {
        stage('Click and Welcome') {
            steps {
                bat 'dir'
                bat 'Click.py'
                bat 'Welcome.py'
            }
        }
    }
}
